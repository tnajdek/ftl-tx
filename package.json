{
  "name": "ftl-tx",
  "version": "0.2.1",
  "description": "Converts .ftl to JSON with ICU Messages and back",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "c8 mocha --parallel",
    "test-watch": "mocha -w --parallel",
    "lint": "eslint \"src/*.js\"",
    "coverage": "c8 --reporter=html --reporter=text mocha --parallel",
    "coverage-make-lcov": "c8 report --reporter=text-lcov > ./coverage/lcov.info"
  },
  "author": "Tom Najdek",
  "license": "AGPL-3.0",
  "files": [
    "src/**/*"
  ],
  "dependencies": {
    "@fluent/syntax": "^0.19.0"
  },
  "devDependencies": {
    "c8": "^7.13.0",
    "chai": "^4.3.7",
    "eslint": "^8.37.0",
    "mocha": "^10.2.0"
  }
}
